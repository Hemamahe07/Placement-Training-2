class CountBits {
    public static void main(String[] args) {
        int n = 5;
        int[] res = new int[n + 1];

        for (int i = 1; i <= n; i++)
            res[i] = res[i >> 1] + (i & 1);

        for (int x : res) System.out.print(x + " ");
    }
}
