def countBinarySubstrings(s):
    prev = cur = res = 0
    for i in range(len(s)):
        cur += 1
        if i+1 == len(s) or s[i] != s[i+1]:
            res += min(prev, cur)
            prev = cur
            cur = 0
    return res
