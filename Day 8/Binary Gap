def binaryGap(n):
    last = res = -1
    i = 0
    while n:
        if n & 1:
            if last != -1:
                res = max(res, i-last)
            last = i
        i += 1
        n >>= 1
    return res
