def nearestValidPoint(x, y, points):
    res = -1
    dist = float('inf')
    for i,(a,b) in enumerate(points):
        if a==x or b==y:
            d = abs(a-x)+abs(b-y)
            if d < dist:
                dist = d
                res = i
    return res
